## Nature Medicine Submission SOP

This document outlines the basic workflow for preparing results from the IBS Reticulotype Project for potential publication.

1. Generate synthetic patient data.
2. Train the RL agent using the environment in `core/ibs_env.py`.
3. Evaluate performance using `evaluation/multi_session_evaluator.py`.
4. Deploy the API and visualization tools.
